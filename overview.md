## **課題**

**問題文：**
`/users` ページにユーザーのリストを表示し、
クリックすると `/users/:id` に遷移して詳細情報を表示してください。

**条件：**
(1) `<Outlet />` を使用すること
(2) 親コンポーネントはユーザーリストを表示し、子ルートで詳細を表示する
(3) TailwindCSS でユーザー名に `hover:text-blue-600` を付けること
